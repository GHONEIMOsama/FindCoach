export default {
  /**
   * Getter for state isLoggedIn.
   * @param {*} state
   * @returns
   */
  getIsLoggedIn(state) {
    return state.isLoggedIn;
  },
  /**
   * Getter for state loginInfos.
   * @param {*} state
   * @returns
   */
  getLoginInfos(state) {
    return state.loginInfos;
  },
};
